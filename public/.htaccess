<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /radiotaxi_viacha_mvc/public/

    # Si no es archivo ni directorio real, redirigir a index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]
</IfModule>
